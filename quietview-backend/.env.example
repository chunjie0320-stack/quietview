# QuietView 后端环境变量示例
# 部署到 Railway 时在项目 Variables 面板填写

# 写入接口鉴权 Key（必须，自己随机生成一个长字符串）
QUIETVIEW_API_KEY=your-secret-key-here

# SQLite 数据库路径（Railway volume 挂载后填绝对路径，否则用默认）
DB_PATH=content.db

# Flask 运行模式（Railway 留 production）
FLASK_ENV=production

# 端口（Railway 自动注入 $PORT，本地开发可填 5000）
# PORT=5000
